import { InputContainer } from './styles'
import { IInputRootProps } from './types'

export const InputRoot = ({ children }: IInputRootProps) => {
  return <InputContainer>{children}</InputContainer>
}
